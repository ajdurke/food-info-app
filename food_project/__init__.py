"""Top-level package for the food information application."""

# This file marks ``food_project`` as a Python package.  Having a
# package lets us organize the code into logical modules such as
# ``database`` and ``ui`` while still being able to import them using
# ``food_project.<module>``.
